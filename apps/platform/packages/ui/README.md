---
name: UI Package
scope: Shared React component library
dependencies:
  - "react"
  - "tailwindcss"
llmInstructions:
  - USE these components for all UI development
  - PREFER existing components over creating new ones
---

# `@dub/ui`

`@dub/ui` is a library of React components that are used across Cliqo's web applications.

## Installation

To install the package, run:

```bash
pnpm i @dub/ui
```
