---
name: Utils Package
scope: Shared utility functions
dependencies: []
llmInstructions:
  - CHECK here before writing custom utilities
  - KEEP functions pure and stateless where possible
---

# `@dub/utils`

`@dub/utils` is a library of utility functions that are used across Cliqo's web applications.

## Installation

To install the package, run:

```bash
pnpm i @dub/utils
```
